node {
    def mvnHome
    stage('Preparation') { // for display purposes
        echo 'pull code'
    }
    stage('Build') {
        echo 'build code'
    }
    stage('Results') {
        echo 'deploy code'
    }
}